import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Proyecto DEU',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
